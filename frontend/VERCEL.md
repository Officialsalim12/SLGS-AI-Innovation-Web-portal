# Vercel deploy

- **Root Directory:** `frontend` (this folder)
- **Framework:** Next.js (set by `vercel.json`)
- **Output Directory:** leave empty — never set `.next` or `out`
- **Env:** `NEXT_PUBLIC_API_URL=https://slgs-ai-innovation-web-portal.onrender.com`
